.DS_Store

# Secrets & host-specific real values, never commit
group_vars/all/vault.yml
group_vars/all/local.yml
hosts/hosts

# Ansible noise
*.retry
.vault_pass

# Local-only material (host-specific values, runbooks, notes), never committed
/personal-server/
